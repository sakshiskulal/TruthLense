{
  "build": {
    "builder": "NIXPACKS",
    "nixpacksPlan": {
      "providers": ["python"]
    }
  },
  "deploy": {
    "startCommand": "gunicorn --worker-class gevent -w 1 --bind 0.0.0.0:${PORT:-8080} run:application"
  }
}
